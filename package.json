{
  "name": "cdnserver",
  "version": "0.0.0",
  "description": "A CDN files server that auto pull from github",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kruceo",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "ffmpeg": "^0.0.4",
    "sharp": "^0.32.1"
  }
}
